version: "3.9"

services:
  verigate:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data  # persist audit/feedback logs across container restarts
    restart: unless-stopped
